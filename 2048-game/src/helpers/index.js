import { initTiles } from "./initTiles";
import { createNewTiles } from "./createTiles";
import { move, directions, combine } from "./moveTiles";
import getTileSize from "./getTileSize";

export { initTiles, createNewTiles, move, directions, combine, getTileSize };
